struct s -> struct client
emialAddress -> emailAddress in struct
removed stdio.h import dupe
stdbool imported
double derefenced pointer formatting
missing i in some for loops (sph)
removed i, j. declared inside functions
function header names more clearer
finding functions changed while into for
finding functions changed old compare into strcmp
free memory function
proper bracketing
FILE f -> FILE file
file found input checking, closes file properly
removed math.h
fixed / improved print statement errors (mainly in command block)
char *val = malloc(100 * sizeof(val[0])) -> char *val = malloc(100 * sizeof(char))
    - many examples for this in the rest of the program~
properly break out of program and free memory if command = 0
int returns -> bool returns
